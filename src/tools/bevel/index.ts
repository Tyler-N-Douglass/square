import { placeholderMount } from '../placeholder';
export const mount = placeholderMount(
  'BEVEL',
  'Hold the phone against an existing cut; capture the angle; get the saw settings to reproduce it.',
  'Gravity mode is only valid when the shared edge is horizontal — the tool checks and refuses otherwise. Test on scrap before cutting the keeper.',
);
