import { placeholderMount } from '../placeholder';
export const mount = placeholderMount(
  'SCAN',
  'Sweep the wall; find the ferrous screw line; infer the stud. Magnetometers find ferrous fasteners, not wood.',
  'It cannot see wood, plastic, or copper. It reads poorly on plaster over lath, glued panels, and metal-stud walls — and it will say so instead of guessing.',
);
