import { placeholderMount } from '../placeholder';
export const mount = placeholderMount(
  'LEVEL',
  'Digital level and plumb with a live camera overlay on the actual object.',
  'A digital level is only as good as its zero. Until the two-position reversal calibration is done, claims stay at ±0.5°.',
);
