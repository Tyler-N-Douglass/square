import { placeholderMount } from '../placeholder';
export const mount = placeholderMount(
  'LAYOUT',
  'Mark one point; get guided to N more at exact equal spacing on a level line.',
  'The overlay is a planning and verification aid, not a substitute for the tape. Measure all marks from the same end — don’t chain.',
);
