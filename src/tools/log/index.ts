import { placeholderMount } from '../placeholder';
export const mount = placeholderMount(
  'LOG',
  'Every measurement saved locally with its uncertainty, a note, and an optional photo. Export JSON, CSV, or a printable job sheet.',
  'All local, IndexedDB, no cloud, ever. Delete-all is one clearly-labeled control away.',
);
